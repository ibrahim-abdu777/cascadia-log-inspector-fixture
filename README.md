# Cascadia Log Inspector

A small Python utility for extracting severity counts from newline-delimited
application logs.

## Review scope

Please review `src/log_inspector.py` for correctness, maintainability, and edge
cases. Run `python -m unittest discover -s tests` to verify behavior.
